<ul>
  <% @users.each do |user| %>
    <li>
      <strong><%= user.email %></strong>
      <% unless current_user == user %>
        <%= link_to '直接发送消息', new_message_path(to: user.id), class: 'btn btn-default btn-sm' %>
      <% end %>
    </li>
  <% end %>
</ul>
